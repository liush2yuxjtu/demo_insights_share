# 复制为 .env 并按需填入：  cp .env.example .env
#
# 两种用法，二选一：
#
# A) 用 MiniMax 高速通道（推荐，demo 启动快，不占用 Claude 订阅额度）
#    把下面 MINIMAX_TOKEN 填上你申请到的真实 token，保存为 .env 即可。
#    token 申请入口：https://api.minimaxi.com/
#
# B) 用自己的 Claude 订阅（或自己的 Anthropic API key）
#    不要创建 .env，保持这里不动即可。start.demo.sh 会自动 fallback 到
#    ~/.claude/.credentials.json（需先执行 `claude login`），或者沿用
#    你 shell 里已 export 的 ANTHROPIC_API_KEY。
#
# 注意：.env 已被 .gitignore 忽略，真实 token 不会被提交进 git。

MINIMAX_TOKEN=sk-cp-PASTE_YOUR_MINIMAX_TOKEN_HERE
