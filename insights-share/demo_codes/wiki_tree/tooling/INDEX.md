# tooling · INDEX

| name | description | trigger when | docs |
|------|-------------|--------------|------|
| 跨平台路径 | 使用 python pathlib 或 bash ${var%/*} 等方式处理，避免硬编码 / 或 \ | 编写跨平台脚本时 | [跨平台路径.md](./跨平台路径.md) |
| html_自动预览 | 必须立即 open -a Google Chrome <abs_path> 弹出预览 | Write/Edit 创建或修改 .html 文件后 | [html_自动预览.md](./html_自动预览.md) |
| 嵌套深度控制 | read_page depth 默认 15，超大结构用 ref_id 聚焦，避免输出膨胀 | 遍历目录树或 JSON 结构时 | [嵌套深度控制.md](./嵌套深度控制.md) |
| 正则表达式斜杠转义 | Grep 工具支持原生正则，Pattern 参数用原生 regex，不需 -E | grep 使用 | 等特殊字符时 | [正则表达式斜杠转义.md](./正则表达式斜杠转义.md) |
| 工作目录持久化 | bash 命令间 cwd 不持久，必须用绝对路径或 cd 后串行 | bash 命令中需要维持当前目录时 | [工作目录持久化.md](./工作目录持久化.md) |
| json_文件逐行处理 | 使用 jq 逐行或流式处理，不要一次性 json.loads 整文件 | 处理 jsonl 或大 JSON 文件时 | [json_文件逐行处理.md](./json_文件逐行处理.md) |
| 长文本分片读取 | 使用 limit/offset 参数读取，避免一次性超载 | 处理超过 25KB 的文件时 | [长文本分片读取.md](./长文本分片读取.md) |
| 循环内文件写入优化 | 累积结果后批量写入，不要逐行 append，减少 I/O | 需要在循环中写多文件时 | [循环内文件写入优化.md](./循环内文件写入优化.md) |
| 格式化验证 | 输出后用 jq/yq/column 等工具验证格式有效 | 生成 JSON/YAML/CSV 输出时 | [格式化验证.md](./格式化验证.md) |
| 相对路径避免 | 始终使用绝对路径，避免 cwd 变化导致错误 | 所有文件操作时 | [相对路径避免.md](./相对路径避免.md) |
| 默认全量并只选 | 不要假设知道搜索目标范围，应全量扫描后再过滤 | grep 搜索不明确时 | [默认全量并只选.md](./默认全量并只选.md) |
| 目录树展示 | 文件/目录变更任务附目录树说明 | 涉及文件结构变更时 | [目录树展示.md](./目录树展示.md) |
| 频繁工具调用优化 | 批量调用独立工具，避免串行等待 | 同一会话内多个独立操作时 | [频繁工具调用优化.md](./频繁工具调用优化.md) |
